using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class JobProcessingPanel : MonoBehaviour
{
    [SerializeField] private Image onGoingSprite;
    [SerializeField] private Image progressBar;
    [SerializeField] private TextMeshProUGUI stack;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
